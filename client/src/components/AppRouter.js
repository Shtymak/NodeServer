import React from 'react';
import {Switch, Redirect, Route} from "react-router-dom";

const AppRouter = () => {
    const isAuth = true
    return (
        <Switch>

        </Switch>
    );
};

export default AppRouter;
